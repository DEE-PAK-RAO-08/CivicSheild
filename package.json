{
  "name": "civicshield-root",
  "version": "2.0.0",
  "description": "CivicShield Root Deployment Package",
  "scripts": {
    "postinstall": "cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && node index.js"
  },
  "engines": {
    "node": ">= 18.0.0"
  }
}
